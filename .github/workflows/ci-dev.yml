name: CI Dev - Build Only

on:
  push:
    branches: [dev]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: |
          cd Dewi-1.0.0
          docker build -t edith-web:dev .
